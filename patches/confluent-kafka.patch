--- a/confluent_kafka/admin/_metadata.py
+++ b/confluent_kafka/admin/_metadata.py
@@ -12,6 +12,10 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+import os
+import socket
+import sys
+
 class ClusterMetadata (object):
     """
     Provides information about the Kafka cluster, brokers, and topics.
@@ -137,7 +141,16 @@ class GroupMember(object):
     def __init__(self,):
         self.id = None
         """Member id (generated by broker)."""
-        self.client_id = None
+        host_name = socket.gethostname()
+        comp_name = os.getenv('SENTRY_COMPONENT')
+        if comp_name:
+            client_name = comp_name
+            comp_ver = os.getenv('SENTRY_RELEASE')
+            if comp_ver:
+                client_name = '%s-%s' % (comp_name, comp_ver)
+        else:
+            client_name = 'python-%d.%d.%d' % sys.version_info[:3]
+        self.client_id = '%s@%s' % (client_name, host_name)
         """Client id."""
         self.client_host = None
         """Client hostname."""
